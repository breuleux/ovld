def __GENERATED__(x):
    return Point(
        x=INJECT(x.x),
        y=INJECT(x.y),
    )

==========
def __GENERATED__(x):
    return Person(
        name=INJECT(x.name),
        hometown=INJECT(x.hometown),
        age=INJECT(x.age),
    )
